// Write your alignTexts function here! ✨
// You'll need to export it so the tests can run it.

type Option = {
    align?: "left" | "middle" | "right";
    width: number;
}

export function alignTexts(text: string, options: Option): string[][] {

}